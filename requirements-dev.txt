# Inclui todas as dependências de runtime + ferramentas de desenvolvimento.
-r requirements.txt

# Lint / format / types
ruff==0.16.7
black==26.5.1
mypy==1.14.1

# Testes
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==7.1.0
httpx==0.28.1
aiosqlite==0.20.0
faker==33.3.1

# Hooks
pre-commit==4.0.1

# Segurança (usados pelo job `quality` do CI: bandit informativo, pip-audit bloqueante)
bandit==1.9.4
pip-audit==2.10.1
